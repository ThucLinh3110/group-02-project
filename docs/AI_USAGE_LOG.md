# AI Usage Log

## Hoàng Luy

| ID | Task | Input/context | AI output | Human verification/decision |
|:---|:---|:---|:---|:---|
| **A-01** | Soạn thảo Glossary hệ thống | Đề bài dự án + Yêu cầu từ BA (`requirements.md`) | Đưa ra danh sách từ vựng kỹ thuật chung. | Nhóm lọc bỏ bớt các từ quá quen thuộc, yêu cầu viết lại và chuẩn hóa các khái niệm đặc thù của dự án. |
| **A-02** | Xây dựng quy tắc Source Priority | Yêu cầu AI đề xuất cách phân xử khi tài liệu trong Vault có thông tin chênh lệch. | AI gợi ý: "Hệ thống sẽ tự động đối chiếu và chọn tài liệu có mốc thời gian cập nhật mới nhất để trả lời". | **Bác bỏ giải pháp:** Quy định này rủi ro vì AI không thể tự quyết định logic hệ thống. Điều chỉnh lại trật tự: **Business Rules > Requirement Inventory > PRD > Glossary**; trường hợp không có dữ liệu thì bắt buộc trả lời **KHÔNG ĐỦ DỮ LIỆU**. |
| **A-03** | Tạo bộ câu hỏi Vault Benchmark | Nạp `rules.md` và `requirements.md`, yêu cầu sinh 20 câu hỏi kiểm thử bao gồm ca bình thường và ca ngoại lệ. | AI sinh đủ 20 câu, nhưng với các câu hỏi nằm ngoài phạm vi kỹ thuật (hỏi giờ cơm trưa, tiền thưởng lễ), AI lại tự tra cứu kiến thức bên ngoài để trả lời. | **Chỉnh sửa ràng buộc:** Bổ sung quy định Hàng rào an toàn (Guardrails). Ràng buộc AI phải nhận biết câu hỏi Out-of-Scope, từ chối trả lời lịch sự và hướng dẫn liên hệ phòng Nhân sự/Hành chính (HR/Admin). |
| **A-04** | Kiểm thử câu hỏi Benchmark (Q20) | Đưa câu hỏi: *"Khách hàng có được tự ý đổi mức độ ưu tiên sau khi vé đã chốt không?"* kèm tài liệu `rules.md`. | Ban đầu AI trả lời: "Được, khách hàng có thể nâng mức độ ưu tiên lên Critical nếu cảm thấy sự cố của mình ngày càng khẩn cấp hơn". | **Bắt lỗi logic & Ảo giác (Hallucination):** Phản hồi này vi phạm quy tắc nghiệp vụ `BR-HD-03`. Nhóm bác bỏ kết quả, ép AI đọc lại điều khoản `BR-HD-03` trong `rules.md`. Sau khi tinh chỉnh context, AI đã trả lời chính xác (*Pass*): "Không, khách hàng không có quyền tự thay đổi Priority sau khi vé đã được AI phân loại hoặc IT Manager chốt". |
| **A-05** | Viết Test tự động cho Module AI (`test_unit_ai.py`) | Yêu cầu AI viết bộ Unit Test cho hàm xử lý AI với thư viện `pytest`. | AI ban đầu chỉ viết test happy path gọi API thật đến Gemini, không mock kết quả, khiến test chạy rất chậm và bị fail khi rớt mạng hoặc hết quota. | **Bác bỏ & Bổ sung Mocking:** Bác bỏ việc gọi API thật trong unit test. Bổ sung `unittest.mock.patch` và `AsyncMock` để cô lập môi trường. Dùng `@pytest.mark.parametrize` sinh 25 ca test biên: JSON lỗi, phản hồi rỗng, độ tin cậy thấp (`confidence < 0.7`), và giả lập lỗi `asyncio.TimeoutError` để kiểm chứng fallback. |
| **A-06** | Thiết kế Prompt RAG & Guardrails cho AI Suggestion (`ai_service.py`) | Yêu cầu AI viết prompt để LLM đọc 20 bài viết trong Knowledge Base và sinh câu trả lời hướng dẫn khách sửa lỗi IT. | AI sinh prompt tự do: cho phép LLM tự suy luận và nếu không tìm thấy bài viết thì tự tra cứu kiến thức bên ngoài để trả lời cho khách. | **Bác bỏ & Thiết lập Guardrail:** Ngăn chặn triệt để ảo giác (Hallucination): Ép thêm quy tắc nghiêm ngặt: *`If the KB articles do not contain the answer... you MUST return EXACTLY the string: NO_MATCH`*. Định dạng chuẩn văn phong Helpdesk: bắt buộc có câu chào mở đầu và câu hỗ trợ kết thúc. |
| **A-07** | Xử lý Ngoại lệ và Cơ chế Fallback khi gọi LLM (`ai.py`) | Viết logic API gọi mô hình Gemini tại endpoint `POST /api/tickets/{id}/ai-suggest`. | AI đề xuất gọi trực tiếp `await generate_ai_draft()` mà không có khối `try...except`, ném thẳng lỗi `500 Internal Server Error` ra ngoài nếu rớt mạng hoặc hết quota. | **Sửa đổi kiến trúc Fallback:** Bọc toàn bộ lệnh gọi LLM trong `try...except`. Khi có lỗi (Timeout/Network/Quota), trả về mã `status="error"` kèm thông báo thân thiện: *"Xin lỗi, hệ thống AI đang quá tải hoặc gặp sự cố kỹ thuật. Vui lòng thử lại sau."* để bảo vệ server không bị crash. |


## Thục Linh

| ID | Task | Input/context | AI output | Human verification/decision |
|:---|:---|:---|:---|:---|
| **A-08** | Phân tích Yêu cầu (Requirement Reviewer) | Nạp ghi chú phỏng vấn (User Research) và yêu cầu rà soát khoảng trống tính năng. | AI đề xuất thêm tính năng "Tự động xóa các vé rác nếu mô tả quá ngắn". | **Bác bỏ:** Việc tự xóa vé là vi phạm quyền của người dùng. Quyết định đưa tính năng tự động xóa vào danh sách Out-of-Scope. |
| **A-09** | Chia User Story (Story Breaker) | Nạp file PRD và yêu cầu lên khung User Story cho hệ thống. | AI đề xuất gom chung luồng "Gán nhãn AI" và "Dashboard Quản lý" thành 1 Story rất lớn. | **Sửa đổi:** Ép AI tách luồng "AI gán nhãn tự động" thành một Story cá nhân độc lập (US-HD-02) để đảm bảo khối lượng công việc nhỏ gọn (≤ 3 points). |
| **A-10** | Viết Tiêu chí nghiệm thu (Acceptance Criteria) | Nạp thẻ US-HD-02, yêu cầu AI viết Acceptance Criteria theo chuẩn Given/When/Then. | AI sinh ra 2 AC nhưng chỉ bao phủ trường hợp thành công (Happy Path). | **Bổ sung:** Yêu cầu AI làm thêm các lỗi ngoại lệ (Error paths). Bổ sung AC2: Nếu mô tả vé quá mơ hồ, AI bắt buộc trả về nhãn "Unknown" thay vì đoán bừa. |
| **A-11** | Xây dựng Bộ luật Phân loại (System Prompt JSON) | Yêu cầu viết cấu trúc Prompt để AI đọc thẻ vé và tự động phân loại Category/Priority. | AI trả về một prompt chung chung, cho phép LLM tự do bịa ra tên Category mới (vd: "Lỗi mạng", "Rớt mạng") làm vỡ Database. | **Bác bỏ & Tự thiết kế (Few-shot prompting):** Em từ chối prompt này và tự thiết kế lại. Em khóa chặt Category vào 5 từ khóa chuẩn (Network, Hardware...) và ép AI trả về định dạng `json_object`. Em cung cấp 5 kịch bản mẫu (Phồng pin, Phishing) để AI học và gán nhãn chính xác 100%. |
| **A-12** | Thiết kế bộ Prompt & Guardrail cho tính năng phân loại vé (AI Triage) | Mô tả nghiệp vụ phân loại thẻ vé (US-HD-02.1) + 5 kịch bản lỗi mẫu thực tế. | AI đề xuất đoạn prompt mẫu và gợi ý cách phân loại kết quả bằng văn bản (text) thông thường. | **[Đã điều chỉnh]** BA trực tiếp review và tinh chỉnh lại. Áp dụng Few-shot prompting và thêm Guardrail ép AI bắt buộc phải trả về định dạng **JSON Schema** nghiêm ngặt để đảm bảo Backend đọc được, không làm sập Database. | 
| **A-13** | Tư vấn kiến trúc tích hợp API Gemini vào hệ thống | Prompt: "Làm cách nào để tích hợp API Gemini vào dự án React + FastAPI nhanh nhất?" | AI gợi ý nhúng trực tiếp code gọi API vào Frontend (React component) để tiết kiệm thời gian code. | **[Reject - Từ chối]** BA quyết định từ chối giải pháp của AI vì vi phạm bảo mật (lộ API Key) và chuẩn Clean Architecture. Chỉ đạo team bóc tách luồng AI xuống Backend (`routers/ai.py` và `services/ai_service.py`). | 
| **A-14** | Xây dựng kịch bản Test E2E (UI) giả lập cơ chế Fallback (Lỗi AI) | Tài liệu đặc tả US-HD-02.2 (Cơ chế phòng vệ) + Yêu cầu chặn bắt API (Mock Data). | AI sinh ra bộ khung code Playwright (`test_e2e_ui.py`), bao gồm các lệnh điều khiển trình duyệt cơ bản (nhập password, click đăng nhập). | **[Đã tinh chỉnh]** Nhóm giữ lại bộ khung thao tác UI, nhưng tự cấu trúc lại phần logic `route.fulfill` để giả lập đứt kết nối mạng. Chạy thử nghiệm thu đảm bảo thẻ vé trả về trạng thái 'Unassigned' đúng 100% theo AC. | 
| **A-15** | Xây dựng cẩm nang vận hành hệ thống (RUNBOOK) | Các lệnh khởi chạy Frontend (`npm`), Backend (`uv`) và cấu hình Docker Compose từ team Dev. | AI hỗ trợ format cấu trúc file Markdown, tạo mục lục (Table of Contents) và highlight các block code bash. | **[Đã kiểm duyệt]** Rà soát lại từng lệnh, tự bổ sung thêm phần hướng dẫn "Khởi tạo dữ liệu cơ sở" (Seed data cho RAG) để sát với thực tế luồng vận hành của dự án. | 
| **A-16** | Tạo dữ liệu mẫu (Sample Data) kiểm thử tính năng RAG | Prompt: "Viết quy trình 3 bước xử lý lỗi máy in kẹt giấy dành cho nhân viên IT Helpdesk." | AI sinh ra đoạn văn bản hướng dẫn chi tiết cách tắt máy, rút giấy và khởi động lại. | **[Đã sử dụng]** BA lọc lại nội dung, định dạng thành file `.txt` và tải lên (Upload) hệ thống Knowledge Base để demo thực tế khả năng tra cứu tài liệu của AI RAG. | 


## Hoa

| ID | Task | Input/Context | AI Output | Human Verification/Decision |
| :--- | :--- | :--- | :--- | :--- |
| **A-17** | Vẽ sơ đồ kiến trúc hệ thống (`architecture.md`) | Yêu cầu dự án + các chức năng AI, Ticket, SLA, Knowledge Base và Notification | AI đề xuất kiến trúc gồm LLM, AI Orchestrator, các Service, API + Auth, PostgreSQL, File Storage và Audit/AI Logs. | **Kiểm tra và điều chỉnh:** Xác nhận luồng LLM → AI Orchestrator → Service và nguyên tắc Human-in-the-loop. |
| **A-18** | Vẽ sơ đồ DB / Data Model (`data-model.md`) | Yêu cầu nghiệp vụ + kiến trúc hệ thống + các chức năng chính | AI đề xuất các entity, fields, relationships và business rules cho hệ thống. | **Review và chuẩn hóa:** Kiểm tra entity, field, relationship và các business rules theo yêu cầu dự án. |
| **A-19** | Liệt kê API (`API.md` – Swagger/Postman) | Data Model + Architecture + yêu cầu authentication/authorization | AI đề xuất các REST API kèm Method, Path, Input, Output và Auth. | **Kiểm tra và hoàn thiện:** Đối chiếu API với Data Model, business flow và quyền của từng role. |
| **A-20** | Backend Authentication | Lỗi khởi động Uvicorn do thư viện `passlib` cũ không tương thích với Python mới. | Đề xuất xóa `passlib`, viết lại hàm mã hóa mật khẩu dùng trực tiếp `bcrypt`. | Kiểm tra và xác nhận code mới, đăng nhập thành công với tài khoản admin/123456. | Sửa dứt điểm lỗi crash hệ thống, bảo mật luồng đăng nhập an toàn hơn. |
| **A-21** | Database Migration | Cần đưa dữ liệu từ PostgreSQL (Docker Local) lên Cloud (Supabase). | Tạo lệnh `pg_dump` để xuất dữ liệu và hướng dẫn chạy query trên Supabase. | Tự tay copy file `dump.sql` chạy trên giao diện SQL của Supabase và kiểm tra bảng. | Chuyển đổi thành công 100% dữ liệu ticket, user từ máy cá nhân lên môi trường thật. |
| **A-22** | Backend Deployment | Render báo lỗi `TypeError` khi kết nối Supabase do xung đột `asyncpg` và PgBouncer. | Đề xuất đổi cổng 6543 thành 5432 và xóa hậu tố `?pgbouncer=true` trong chuỗi kết nối. | Cập nhật `DATABASE_URL` trên Render Environment và Redeploy. | Máy chủ Backend (Render) kết nối mượt mà với Database, hoạt động ổn định trên mạng. |
| **A-23** | Frontend Build | Vercel không cho Build do các cảnh báo gắt gao của TypeScript (`tsc -b`). | Đề xuất sửa script trong `package.json` để bỏ qua bước check TypeScript khi Build. | Xem xét và đồng ý để AI thay đổi code trong `package.json`, push lên Github. | Đưa thành công Frontend lên Vercel mà không bị nghẽn bởi các lỗi strict type. |
| **A-24** | API Integration | Frontend trên Vercel báo "Lỗi kết nối máy chủ" (lỗi 404 API). | Phát hiện thiếu hậu tố `/api`, viết code fix cứng (hardcode) URL Render vào Axios. | Test trực tiếp trên web Vercel, xác nhận tính năng Đăng nhập đã hoạt động. | Thông luồng kết nối Frontend-Backend, hoàn thiện hệ thống trên Production. |
| **A-25** | UX/Routing Fixes | Vercel bị lỗi 404 khi nhấn F5 và ảnh đính kèm bị lỗi hiển thị. | Tạo file cấu hình `vercel.json` (SPA Rewrite) và đổi link ảnh từ `localhost` sang Render. | Xác nhận code mới, tạo thử Ticket mới để kiểm tra chức năng upload & reload trang. | Xử lý triệt để lỗi điều hướng (Routing) kinh điển của SPA và sửa lỗi hiển thị tài nguyên. |

---

##  Ngọc

| ID | Task | Input/Context | AI Output | Human Verification/Decision |
| :--- | :--- | :--- | :--- | :--- |
| **A-26** | Lập bảng Traceability Matrix (Traceability) | Bảng phân công + hướng dẫn thực thi của nhóm trưởng (cột: Mã REQ, Link Taiga, Link Figma, API, Pass/Fail); 5 Story của nhóm. | Đề xuất bảng 8 cột cho 5 Story, tự điền sẵn dòng Story 5 (do Ngọc phụ trách), các dòng Story 1-4 để trống dạng placeholder. | Chấp nhận cấu trúc cột; kiểm tra placeholder được đánh dấu rõ (màu vàng) để không nhầm là dữ liệu thật. Yêu cầu bổ sung sheet Definition of Ready đi kèm. |
| **A-27** | Duyệt Acceptance Criteria (Definition of Ready) | 5 thẻ Story dự kiến của nhóm (Tạo ticket, AI phân loại, AI phản hồi, SLA, Knowledge Base). | AI đề xuất bộ AC mẫu theo Given-When-Then cho từng Story, dùng để đối chiếu khi duyệt thẻ thật trên Taiga. | **Bổ sung:** Chỉ dùng làm bản đối chiếu tạm thời; AC thật phải do chủ luồng viết và Ngọc duyệt lại trên Taiga, không copy thẳng bản AI làm vào backlog chính thức. |
| **A-28** | Viết chiến lược Test Automation (Test Strategy) | Yêu cầu nhóm trưởng: 'Viết chiến lược Test Automation'; 5 API endpoint dự kiến của các Story. | AI đề xuất chỉ automation phần API (Postman Collection + Tests script + Collection Runner), giữ UI test tay vì mới ở giai đoạn Figma prototype. | Đồng ý giới hạn phạm vi automation; tự thêm ghi chú giới hạn (không automation UI, không tự động chấm chất lượng câu trả lời AI) để không bị hỏi vặn khi báo cáo. |
| **A-29** | Lập 10 Test Case cho Story 5 và toàn dự án | AC của 5 Story; mẫu bảng Test Case tham khảo từ giáo trình (ID \| Case \| Trace \| Expected \| Mode). | AI sinh 10 test case TC-01..TC-10 phủ đủ 5 Story, có cột Trace nối về đúng REQ/AC tương ứng. | Đối chiếu từng test case có Trace hợp lệ, không có case 'mồ côi' không rõ nguồn. Giữ TC-10 (Knowledge Base không khớp) ở chế độ Manual/E2E vì cần quan sát trực quan UI. |

---

##  Ngân

| ID | Task | Input/Context | AI Output | Human Verification/Decision |
| :--- | :--- | :--- | :--- | :--- |
| **A-30** | Đưa ra 4 flow rủi ro cao | Dựa vào user story | Xây dựng 4 flow: <br>- A: Tạo ticket + AI phân loại<br>- B: Low-confidence<br>- C: Agent xử lý và AI Reply<br>- D: Verify & Close | Kiểm tra lại flow và yêu cầu giữ đúng các state bắt buộc: `loading`, `empty`, `AI-classifying`, `low-confidence`, `no-agent-available`, `network-error`, `draft-before-submit`, `success`. |
| **A-31** | Xây dựng Prototype Brief | Tạo một prototype web responsive cho Voice Commerce.<br>- Dùng requirement và business rules.<br>- Tập trung 4 flows trong Prototype Brief.<br>- Hiển thị rõ transcript, assistant message, product cards, cart summary và confirmation. | Prototype đã hiển thị có thể đổi role ("Xem như") để duyệt qua cả 4 flow: Employee, Agent, IT Manager. | Giữ lại các luồng cơ bản, bổ sung thêm các luồng và chỉnh sửa bố cục hợp lý. |
| **A-32** | Xây dựng Design System | Prototype Brief | AI đưa ra bảng quy định chuẩn: Design Tokens, Component Inventory, UX Copy dựa vào prototype. | Chấp nhận quy định màu, kiểm tra lại Figma thực tế, đặc biệt Button size và ảnh component, trước khi merge. AI không tự quyết định giá trị Figma chưa được xác nhận. |
| **A-33** | Lập trình Component đếm ngược SLA (`SLABadge.tsx`) | Thiết kế component hiển thị thời gian đếm ngược SLA realtime trên từng ticket (`US-HD-04.1`). | AI viết code React hook dùng `setInterval` để cập nhật thời gian đếm ngược mỗi giây một lần. | **Bắt lỗi Memory Leak & Race Condition:** AI quên viết hàm dọn dẹp (cleanup function) trong `useEffect`. Nếu người dùng chuyển trang liên tục, các timer chạy ngầm sẽ gây tràn bộ nhớ và giật lag giao diện. Ngân đã can thiệp bổ sung `return () => clearInterval(timer)` để hủy timer an toàn khi component unmount (Khớp bằng chứng `code-review.md`). |
| **A-34** | Thiết lập Quy tắc đổi màu cảnh báo SLA theo thời gian thực | Nghiệp vụ `US-HD-04.1` và `BR-HD-01`: Cảnh báo trực quan cho IT Agent khi vé sắp trễ hạn. | AI ban đầu chỉ chia 2 màu cơ bản: Màu Xanh khi còn hạn và Màu Đỏ khi quá hạn. | **Bổ sung trạng thái At Risk:** Ngân bác bỏ vì chưa đủ cảnh báo sớm. Bổ sung thêm trạng thái **At Risk (Màu Vàng/Cam)** khi thời gian còn ≤ 25% hoặc ≤ 1 giờ. Khi vé bị `Breached` (Đỏ), kích hoạt thêm class hiệu ứng nhấp nháy `animate-pulse` để thu hút sự chú ý khẩn cấp của IT Agent. |
| **A-35** | Tối ưu hóa Bộ lọc Dashboard SLA (`Dashboard.tsx`) | Thiết kế logic lọc ticket theo các trạng thái SLA (On track, At risk, Breached) trên Dashboard (`US-HD-04.2`). | AI viết hàm filter vô tình loại bỏ luôn các ticket đã hoàn thành (`Resolved`/`Closed`) khi người dùng bấm xem thẻ tổng hợp "All Tickets". | **Sửa lỗi lọc dữ liệu (Fix BUG-03):** Ngân phát hiện lỗi logic khiến người quản lý lầm tưởng bị mất dữ liệu vé cũ. Đã sửa lại điều kiện lọc `if (filter === 'all') return true;` để hiển thị đầy đủ toàn bộ lịch sử vé của hệ thống. |


